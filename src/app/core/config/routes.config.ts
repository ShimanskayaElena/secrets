export enum AnimationRoutes {
  Counter = 'counter',
  Counters = 'counters'
}

export enum ExampleRoutes {
  Parent = 'parent',
  Child = 'child',
  Singleton = 'singleton',
  SingletonSecond = 'singleton-second',
}
